# Zync Desktop (Lua)

> **⚠️ Experimental — not part of the official release.**
>
> A minimal Lua client (Lua 5.3). No packaging, signing, or CI build exists, so
> it does not ship as a supported desktop app. Keep only as a reference for a
> future native desktop client.
