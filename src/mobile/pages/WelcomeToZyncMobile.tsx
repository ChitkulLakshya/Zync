// Imports the primary WelcomeToZync component from the shared pages directory.
import WelcomeToZync from "@/pages/WelcomeToZync";

// Defines a lightweight wrapper component for mobile routing to display the welcome onboarding flow.
const WelcomeToZyncMobile = () => <WelcomeToZync />;

// Exports the mobile wrapper for the router.
export default WelcomeToZyncMobile;
