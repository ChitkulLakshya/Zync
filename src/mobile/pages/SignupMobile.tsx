// Imports the primary Signup component from the shared pages directory.
import Signup from "@/pages/Signup";

// Defines a lightweight wrapper component for mobile routing to display the registration flow.
const SignupMobile = () => <Signup />;

// Exports the mobile wrapper for the router.
export default SignupMobile;
