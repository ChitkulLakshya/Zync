// Imports the primary ProjectDetails component from the shared pages directory.
import ProjectDetails from "@/pages/ProjectDetails";

// Defines a lightweight wrapper component for mobile routing to display the specific project view.
const ProjectDetailsMobile = () => <ProjectDetails />;

// Exports the mobile wrapper for the router.
export default ProjectDetailsMobile;
