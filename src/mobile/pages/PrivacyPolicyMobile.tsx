// Imports the primary PrivacyPolicy component from the shared pages directory.
import PrivacyPolicy from "@/pages/PrivacyPolicy";

// Defines a lightweight wrapper component for mobile routing to display the detailed privacy policy view.
const PrivacyPolicyMobile = () => <PrivacyPolicy />;

// Exports the mobile wrapper for the router.
export default PrivacyPolicyMobile;
