// Imports the primary Terms component from the shared pages directory.
import Terms from "@/pages/Terms";

// Defines a lightweight wrapper component for mobile routing to display the terms of service view.
const TermsMobile = () => <Terms />;

// Exports the mobile wrapper for the router.
export default TermsMobile;
