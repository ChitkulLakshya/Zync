// Imports the primary Privacy component from the shared pages directory.
import Privacy from "@/pages/Privacy";

// Defines a lightweight wrapper component for mobile routing to display the privacy view.
const PrivacyMobile = () => <Privacy />;

// Exports the mobile wrapper for the router.
export default PrivacyMobile;
