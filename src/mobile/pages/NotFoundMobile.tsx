// Imports the primary NotFound component from the shared pages directory.
import NotFound from "@/pages/NotFound";

// Defines a lightweight wrapper component for mobile routing to handle 404 errors.
const NotFoundMobile = () => <NotFound />;

// Exports the mobile wrapper for the router.
export default NotFoundMobile;
